# 📋 Google Forms Lite Clone

Спрощений клон Google Forms із підтримкою створення форм, їх заповнення та перегляду результатів. Реалізований як монорепозиторій з окремими пакетами для клієнта та сервера

---

## 🚀 Швидкий запуск

Всі команди виконуються з **кореневої папки** проекту

### 1. Встановити залежності
```bash
npm install
```

Це встановить залежності для root, client та server завдяки npm workspaces

### 2. Запустити проект
```bash
npm run dev
```

Запускає клієнт та сервер одночасно через `concurrently`

| Сервіс | URL |
|--------|-----|
| Frontend | http://localhost:5173 |
| Backend (GraphQL) | http://localhost:4000 |

### 3. Оновити GraphQL типи (опціонально)

Якщо ви змінили схему на сервері — оновіть типи на клієнті:
```bash
npm run codegen
```

---

## 🛠 Технології

- **Frontend:** React, TypeScript, Redux Toolkit, RTK Query
- **Backend:** Node.js, Apollo Server, GraphQL
- **Styling:** Tailwind CSS
- **Monorepo:** npm workspaces + concurrently
- **Code Generation:** @graphql-codegen для автоматичної генерації TypeScript типів із GraphQL схеми

---

## 📂 Структура проекту
```
google-forms-clone/
├── client/         # React додаток (Vite)
├── server/         # Apollo Server + GraphQL API
├── package.json    # Root package.json з workspaces
└── README.md
```

---

## ✨ Функціонал

- **`/`** — Головна сторінка зі списком усіх форм
- **`/forms/new`** — Конструктор форм з типами питань: `TEXT`, `DATE`, `MULTIPLE_CHOICE`, `CHECKBOX`
- **`/forms/:id/fill`** — Заповнення форми та відправка відповідей
- **`/forms/:id/responses`** — Перегляд усіх відповідей по формі

---

## 📌 Примітка

Дані зберігаються в пам'яті сервера (in-memory store) і скидаються після його перезапуску